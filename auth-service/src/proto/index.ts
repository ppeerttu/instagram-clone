import "./generated/auth_service_grpc_pb";
import "./generated/auth_service_pb";

/**
 * Here we just define an empty function that can be include into the typescript
 * compilation.
 */
export const protoIndex = (): void => { /* NOOP */ };
